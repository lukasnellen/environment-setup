# -*- shell-script -*-
# completions for conda environments
source ${HOME}/.setup.d/py.def
_list=$(${CONDA_PREFIX}/bin/conda env list | awk '/^[^# \t]/ {print $1}')
echo $(compgen -W "${_list}" -- "${1}")
